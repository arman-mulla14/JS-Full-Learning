let arr = [1, 2, 3, 4, 5];



console.log(arr.some(arr => arr > 3)); // Output: true

// that will remove the first element of the array and return it, and the original array will be modified to [2, 3, 4, 5]