let arr = [1, 2, 3, 4, 5];
// using join() to join the elements of the array into a string
// that will join the elements of the array into a string, and it will return the joined string without modifying the original array
let str = arr.join("-"); 
console.log(str); // Output: "1-2-3-4-5"