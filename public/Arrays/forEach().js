let arr = [1, 2, 3, 4, 5];
// using foreach() you can print the whol array element one by one

arr.forEach(num=>{
    console.log(num);
})

let names = ["Alice", "Bob", "Charlie", "David"];
// using foreach() to print the array element one by one
names.forEach(name =>{
    console.log(name); 
})