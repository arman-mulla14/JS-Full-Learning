let revers = [1, 2, 3, 4, 5];
// using reverse() to reverse the order of the elements in the array
// that will reverse the order of the elements in the array, and it will modify the original array
revers.reverse();
console.log(revers); // Output: [5, 4, 3, 2, 1]
