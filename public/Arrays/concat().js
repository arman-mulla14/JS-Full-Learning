
let marge = [1, 2, 3].concat([4, 5, 6]);
console.log(marge); // Output: [1, 2, 3, 4, 5, 6]
// that will merge two or more arrays into a single array, and it will return a new array without modifying the original arrays
    let arr = [1, 2, 3].concat([34,67][22,56]); 
    console.log(arr); // Output: [1, 2, 3, 34, 67, 22, 56]