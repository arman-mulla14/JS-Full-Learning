let arr = [1, 2, 3, 4, 5];
//usnign some() to check if at least one element in the array satisfies a condition 
// that will return true if at least one element in the array satisfies the condition, and false if no elements satisfy the condition

